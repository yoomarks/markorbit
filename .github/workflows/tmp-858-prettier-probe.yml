name: retired-tmp-858-prettier-probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired temporary formatting probe. Never merge this branch."
