name: Temporary M11 WP04 formatter

on:
  push:
    branches:
      - agent/m11-wp04-management-recommendations

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/m11-wp04-management-recommendations
      - uses: pnpm/action-setup@v4
        with:
          version: 10.28.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write services/lite/src/trademark-asset-management-recommendation.ts services/lite/tests/trademark-asset-management-recommendation.test.ts docs/planning/MO-MVP-MILESTONE-011-WP04-NOTES.md
      - name: Commit canonical formatting
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "markorbit-ci"
          git config user.email "markorbit-ci@users.noreply.github.com"
          git add -- services/lite/src/trademark-asset-management-recommendation.ts services/lite/tests/trademark-asset-management-recommendation.test.ts docs/planning/MO-MVP-MILESTONE-011-WP04-NOTES.md
          git commit -m "style: apply exact M11 WP04 formatting"
          git push origin HEAD:agent/m11-wp04-management-recommendations
