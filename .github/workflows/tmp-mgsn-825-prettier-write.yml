name: Temporary MGSN 825 Parser Fix Staging

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staged:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Parser fix helper is staged; push trigger intentionally disabled."
