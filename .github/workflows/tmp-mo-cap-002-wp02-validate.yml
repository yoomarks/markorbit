name: Temporary MO-CAP-002 WP02 Validate

on:
  push:
    branches:
      - mo-cap-002-wp02-ai-gateway-boundary-stacked

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: mo-cap-002-wp02-ai-gateway-boundary-stacked
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 10.28.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Apply canonical formatting
        run: pnpm exec prettier --write packages/ai/src/index.ts packages/ai/tests/provider-registry.test.ts
      - name: Commit formatting
        run: |
          if ! git diff --quiet -- packages/ai; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add packages/ai/src/index.ts packages/ai/tests/provider-registry.test.ts
            git commit -m "CI: format AI Gateway boundary"
            git push origin HEAD:mo-cap-002-wp02-ai-gateway-boundary-stacked
          fi
      - name: Test AI package
        run: pnpm --filter @markorbit/ai test
      - name: Typecheck AI package
        run: pnpm --filter @markorbit/ai typecheck
      - name: Lint AI package
        run: pnpm --filter @markorbit/ai lint
      - name: Build AI package
        run: pnpm --filter @markorbit/ai build
