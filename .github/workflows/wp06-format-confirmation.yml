name: WP06 format confirmation

on:
  push:
    branches: [m3-wp-06-markreg-order-journey]

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: m3-wp-06-markreg-order-journey
      - uses: pnpm/action-setup@v4
        with: { version: 10.28.1 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write apps/markreg-web/src/CustomerConfirmationOrderEntry.tsx
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/markreg-web/src/CustomerConfirmationOrderEntry.tsx
          git commit -m "style(m3): format governed confirmation entry"
          git push origin HEAD:m3-wp-06-markreg-order-journey
