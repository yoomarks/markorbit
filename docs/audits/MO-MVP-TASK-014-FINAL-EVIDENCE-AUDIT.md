# MO-MVP-TASK-014 Final Evidence Audit

Audited head: `94719845345e4139f3a1ee494fb83390ddef2617`.

## Conclusions

- **M-002 remains RESOLVED.** This audit did not re-open the built Storybook evidence.
- **M-003 returns to REMEDIATION_IN_PROGRESS.** The six real-runtime checks prove canonical exact-ID/version reloads and browser-observed GET-only traffic, but they do not read active record IDs/counts before and after navigation. GET-only observation is not sufficient evidence that the authoritative stores contain no duplicate record.
- **M-004 returns to REMEDIATION_IN_PROGRESS.** `pnpm test:negative-path-matrix` is mode **B**: it runs the complete MarkReg, Execution and Gateway package suites, then checks that registry IDs and test-name substrings exist. The registry contains metadata, not arrange/invoke/read/assert executors. The validator does not associate a particular executed assertion with a descriptor, does not support genuine per-case execution, and previously printed unsupported `17 ... passed` claims.
- **Remote Node 22 is UNVERIFIED.** This checkout has no `origin`, PR URL, remote head, Actions URL, or remote status to inspect.

## Per-case executable evidence audit

“Listener” means the referenced Gateway test starts real owning-service and Gateway HTTP listeners. “Complete” requires the full requested Service and Gateway assertion set, not merely a related test.

| Case                                                | Owning service | Service evidence (invocation → error; state/partial/authority audit)                                                                                                                                                                   | Gateway evidence (method/path; listener; HTTP assertions; post-read audit)                                                                                                                                                                                            | Complete |
| --------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| NP-001 stale Quote                                  | MarkReg        | `matter-flow.test.ts` — “rejects a quote-version mismatch and an invalid Quote status”; calls `MatterFlowService.confirm`; only asserts `MatterFlowError`, not `QUOTE_NOT_CONFIRMABLE`, immutable state, partial records, or authority | `vertical-slice.test.ts` — “rejects expired, superseded, and unknown Quotes without side effects”; real stack, POST `/v1/markreg/quotes/:quoteId/confirm`; related HTTP checks, but registry does not execute/select this case or prove descriptor code `STALE_QUOTE` | No       |
| NP-002 expired Quote                                | MarkReg        | Same Service test as NP-001; its arranged invalid status is `SUPERSEDED`, not expired                                                                                                                                                  | `customer-confirmation-matter-draft.test.ts` parameterized “rejects non-confirmable or expired Quotes”; real listeners and POST confirmation status 409, but no exact Gateway error-code or post-failure authoritative read assertion                                 | No       |
| NP-003 Customer Confirmation version mismatch       | MarkReg        | `matter-flow.test.ts` exact quote-version mismatch calls `confirm` and asserts `QUOTE_VERSION_MISMATCH`; descriptor instead claims generic `VERSION_MISMATCH`; no state/partial/authority assertions                                   | Gateway “verifies the exact Quote version”; real POST `/api/markreg/customer-confirmations`, status 409 only; no exact code/post-read                                                                                                                                 | No       |
| NP-004 withdrawn Customer Confirmation              | MarkReg        | `matter-flow.test.ts` calls `createDraft` and asserts `CONFIRMATION_WITHDRAWN`; no unrelated/idempotency/authority assertion                                                                                                           | Gateway POST `/api/markreg/matter-drafts`; real listeners, status/code assertion exists; no full post-failure record-set assertion                                                                                                                                    | No       |
| NP-005 Matter Draft blocking UNKNOWN                | MarkReg        | `matter-flow.test.ts` evaluates readiness and asserts an UNKNOWN check/state; it is not an exception matching descriptor `BLOCKING_CHECK_UNKNOWN`                                                                                      | Gateway PATCH/POST Matter Draft readiness test observes FAIL/UNKNOWN; no matching typed error or post-failure mutation assertion                                                                                                                                      | No       |
| NP-006 duplicate Professional Review Case           | Execution      | `professional-review.test.ts` calls `ProfessionalReviewService.create` twice and asserts `ACTIVE_REVIEW_CASE_EXISTS`; no full immutable/partial/authority set                                                                          | `professional-review-negative-paths.test.ts`; real Execution + Gateway listeners; POST `/api/lite/professional-review-cases`, status 409 and code asserted; no authoritative post-failure list/count or authority assertion                                           | No       |
| NP-007 stale Professional Review                    | Execution      | `professional-review.test.ts` calls stale completion and asserts `CASE_STALE`; source/state checks are distributed across adjacent tests                                                                                               | Real listeners; GET reconciles stale then POST `/complete`, status 409/code `CASE_STALE`, before/after review equality asserted; no partial/authority/idempotency assertion                                                                                           | No       |
| NP-008 missing required document                    | MarkReg        | `preparation.test.ts` evaluates a package and asserts `NEEDS_DOCUMENTS`; descriptor claims `REQUIRED_DOCUMENT_MISSING`, which is not asserted                                                                                          | Real document-package Gateway test observes validation state through POST evaluate; not a matching typed-error case and lacks full post-failure audit                                                                                                                 | No       |
| NP-009 superseded document active evidence          | MarkReg        | Service test asserts explicit supersession behavior/history, not an attempted use of superseded evidence producing descriptor `SUPERSEDED_DOCUMENT`                                                                                    | Gateway document test creates/patches/supersedes; does not attempt the stated prohibited action or assert the descriptor error                                                                                                                                        | No       |
| NP-010 unconfirmed Instruction Ledger               | MarkReg        | Registry points to successful immutable-lock test, not an unconfirmed-ledger rejection executor                                                                                                                                        | Gateway POST `/api/markreg/preparation-locks` test rejects incomplete inputs, but registry does not establish exact `INSTRUCTION_LEDGER_UNCONFIRMED`, complete post-read, or authority assertions                                                                     | No       |
| NP-011 stale Preparation Lock source                | Execution      | Service “rejects an invalid Preparation Lock state” asserts a related source error, not descriptor `STALE_PREPARATION_SOURCE`                                                                                                          | Gateway authorization creation test checks invalid lock/version, but not the exact descriptor plus post-failure state set                                                                                                                                             | No       |
| NP-012 missing Filing Authorization acknowledgement | Execution      | `filing-authorization.test.ts` calls confirmation and asserts `MANDATORY_ACKNOWLEDGEMENT_MISSING`; no full immutable/idempotency/authority post-audit                                                                                  | Real POST confirmation; related acknowledgement rejection is exercised, but the registry does not prove all required post-failure assertions                                                                                                                          | No       |
| NP-013 expired Filing Authorization                 | Execution      | Registry incorrectly points to “withdraws without implying submission”; it does not arrange expiration                                                                                                                                 | Gateway GET reconciles elapsed authorization to `EXPIRED`; this is a state read, not the descriptor’s attempted mutation/error assertion                                                                                                                              | No       |
| NP-014 Execution Release blocking FAIL              | Execution      | Registry incorrectly points to Professional Review blocking FAIL, not Execution Release                                                                                                                                                | Gateway release suite changes the lock and observes `EXECUTION_RELEASE_IMMUTABLE`; it does not assert descriptor `BLOCKING_CHECK_FAILED` or the complete post-failure set                                                                                             | No       |
| NP-015 Execution Release blocking UNKNOWN           | Execution      | Service calls `release` before evaluation and asserts `RELEASE_CHECKS_BLOCKING`; partial/idempotency/authority assertions are incomplete                                                                                               | Real POST `/api/execution/execution-releases/:id/release`, status 422; exact body code/post-read/full authority set not all asserted in this case                                                                                                                     | No       |
| NP-016 duplicate active Execution Release           | Execution      | Registry points to successful single Task Draft creation, not duplicate release rejection                                                                                                                                              | Real second POST release creation and status 409; exact `ACTIVE_EXECUTION_RELEASE_EXISTS` body and complete unchanged-record audit are not asserted                                                                                                                   | No       |
| NP-017 stale Filing Execution Task Draft            | Execution      | Registry points only to creating one PREPARED Task Draft; it does not stale the source or read STALE                                                                                                                                   | Gateway test creates PREPARED, changes lock, GETs release then Task Draft and asserts both STALE. It does not explicitly assert not FILED/not SUBMITTED, unchanged Task Draft count, or equivalent direct Service result                                              | No       |

## Test-count explanation

The small deltas (MarkReg 16 unchanged, Execution 30→31, Gateway 53→56) do **not** introduce 34 new matrix executions. Most registry rows point to pre-existing broadly related tests; NP-006/NP-007 account for two new Gateway tests, and the legacy execution boundary accounts for the one new Execution test. No parameterized runner emits all 17 descriptor IDs, and no runner log binds each descriptor to a Service and Gateway executor.

## Deep-link duplicate-record audit

The checkpoint helper creates a new page, serializes the application route, performs `goto` and `reload`, asserts exact URL/ID/version/status, and records Gateway methods. It does **not** read an active-record collection or service fixture summary before and after. Therefore the following required evidence is absent for Matter Draft, Professional Review Case, Preparation Lock, Filing Authorization, Execution Release and Filing Execution Task Draft:

```text
before: active IDs/count
post-goto/reload: same active IDs/count
```

The current GET-only assertion is useful mutation-request evidence but cannot, by itself, establish authoritative record-count equality.

## Runtime harness audit

The harness registers children before readiness, supports custom ports, uses non-detached children for the Playwright launcher, awaits SIGTERM/exit and bounded SIGKILL, closes logs, checks port release and provides idempotent stop. Standalone mode retains detached process-group cleanup. The regression suite exercises occupied port, middle and Web failure, timeout, named early exit, double stop, custom ports and successful six-runtime release. Signal handlers are process-scoped and `once`; process termination removes them, but no long-lived in-process start/stop API is exposed by the launcher.

## Clean-checkout negative-path entry correction

The contracts package correctly declares `main` and ESM `exports.import` as `./dist/index.js`, with declarations at `./dist/index.d.ts`; its `tsup` build produces those two files. A detached clean worktree proved that `pnpm install --frozen-lockfile` does not produce `dist`, so importing `@markorbit/contracts` from MarkReg failed with `ERR_MODULE_NOT_FOUND`. Running the existing topological Turbo build made the same import pass. The failure was missing dependency preparation, not an incorrect package export.

`test:negative-path-matrix` now invokes `build:negative-path-deps`, which topologically builds Contracts, Events, Service Kit, MarkReg, Execution and Gateway before selecting evidence tests. `--case NP-006` was also verified in that clean worktree. The new runner selects exactly one registered Service test and one registered Gateway test for `--case`; a local loop selected all 17 IDs independently.

This correction makes the command clean-checkout safe and changes it from broad-suite mode to selected evidence-test execution. It does **not** cure the semantic gaps in the table above: several registrations still point to only related tests rather than complete descriptor-specific assertions. Therefore M-004 remains **REMEDIATION_IN_PROGRESS**, and runner output deliberately says “referenced test PASS” rather than claiming 17 complete Service/Gateway cases.

## M-003 authoritative repository closure

The prior active-record evidence gap is closed by a protected, GET-only scenario snapshot that composes direct reads from both owning in-memory repositories. Each real-runtime checkpoint compares all six collections before `goto` and after reload, including total and active counts, active IDs, exact record IDs, versions, statuses, source lineage and a complete-record content hash that covers acknowledgements and internal assignments. The desktop path passed in 38.0 seconds and the 390px mobile path passed in 33.1 seconds, both on the first attempt with retries disabled. Six of six snapshots were equal in each project, no mutation method was observed, duplicate active-record change was zero, and thirteen of thirteen authority consequences remained false. M-003 is therefore **RESOLVED**; M-004 remains **REMEDIATION_IN_PROGRESS**.

## TASK 014-C2-B final semantic evidence

The earlier M-004 correction is now superseded by descriptor-specific execution. All 17 descriptors have individually executable public Service tests and real Gateway-to-owning-service HTTP tests. MarkReg is 9/9 and Execution is 8/8. Each test validates the descriptor code/status/stage, authoritative post-state, no partial downstream record, idempotency integrity and 13/13 false authority consequences. NP-017 retains exactly one Task Draft with the original ID and changes only its status from `PREPARED` to `STALE`; it explicitly rejects `FILED` and `SUBMITTED`. M-004 is locally **RESOLVED**; remote Node 22 validation is **UNVERIFIED**. The original result remains **FAIL** and freeze recommendation **DO_NOT_FREEZE**.
