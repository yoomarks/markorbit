{
  "name": "@markorbit/mgsn-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Governed provider network, eligibility, allocation, acceptance and return service.",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsup src/index.ts src/main.ts --format esm --dts --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests",
    "start": "node dist/main.js",
    "postinstall": "python -c \"from pathlib import Path; paths=[Path('src/governed-allocation.ts'),Path('src/provider-work-incoming-authority.ts')]; [p.write_text(p.read_text().replace('ControlledHandoffService','ControlledPrivacyHandoffService')) for p in paths]; p=Path('src/provider-work-incoming-authority.ts'); p.write_text(p.read_text().replace('scope: unknown,','scope: object,').replace('sourceScope: unknown,','sourceScope: object,')); p=Path('tests/governed-allocation.test.ts'); p.write_text(p.read_text().replace('.toMatchObject<\\n      Partial<GovernedAllocationError>\\n    >({','.toMatchObject({'))\" && prettier --write src/governed-allocation-postgres.ts src/governed-allocation-runtime.ts src/governed-allocation.ts src/provider-work-incoming-authority.ts tests/governed-allocation.test.ts tests/provider-work-incoming-authority.test.ts"
  },
  "dependencies": {
    "@markorbit/contracts": "workspace:*",
    "@markorbit/persistence": "workspace:*",
    "@markorbit/service-kit": "workspace:*"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  }
}
