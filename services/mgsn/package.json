{
  "name": "@markorbit/mgsn-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Governed provider network, eligibility, allocation, acceptance and return service.",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsup src/index.ts src/main.ts --format esm --dts --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests",
    "postinstall": "prettier --write tests/durable-runtime-governed-postgres.test.ts && git diff -- tests/durable-runtime-governed-postgres.test.ts",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@markorbit/contracts": "workspace:*",
    "@markorbit/persistence": "workspace:*",
    "@markorbit/service-kit": "workspace:*"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  }
}
